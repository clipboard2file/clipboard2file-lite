{
  "name": "clipboard2file",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "mocha tests/*.test.js --timeout 30000 --slow 1000",
    "release": "git archive --format=zip -o \"$(jq -r '.name + \"-\" + .version + \".zip\"' manifest.json)\" HEAD"
  },
  "devDependencies": {}
}
